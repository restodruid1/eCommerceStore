

export function CustNailProd(){

    // const location = useLocation();
    // const { onClick, clicked, desktop } = location.state || {};

    return (
        <>
        {/* <NavBar onClick={onClick}/> */}
            <h1>Custom Nail Products</h1>
        </>
    );
}